import React from 'react';
import styles from './ButtonEqualle.module.scss';
import { Button } from '../index';

export const ButtonEqualle = () => {
    return (
        <div className={styles.buttonEquale}>
            <Button value="=" />
        </div>
    );
};
